version: '3.8'

services:
  eshop:
    image: eshop
    container_name: eshop-app
    ports:
      - "8080:8080"
    environment:
      DB_URL: jdbc:mysql://host.docker.internal:3306/gamudalk
      DB_USER: root
      DB_PASSWORD: DEShV*0202*
